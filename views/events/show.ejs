<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/events.css">
	<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Raleway">
</head>
<body>
    <h1>Your Events</h1>
    <hr>
    <nav>
        <a href="/events/new/<%=user.id%>">Add a New Event</a>
    </nav>
    <hr>
    <% for (let i = 0; i < user.Events.length; i++) { %>
            <form action="/events/show/<%= user.Events[i].id %>?_method=PUT" method="POST">    
        
            Title: <input type="text" name="title" value= "<%= user.Events[i].title %>" /><br> <br>
            Location: <input type="text" name="location" value= "<%= user.Events[i].location %>" /><br> <br>
            Start Date: <input type="date" name="startDate" value= "<%= user.Events[i].startDate %>" /><br> <br>
            Start Time: <input type="time" name="startTime" value=<%= user.Events[i].startTime %> /><br> <br>
            End Date: <input type="date" name="endDate" value= "<%= user.Events[i].endDate %>" /><br> <br>
            End Time: <input type="time" name="endTime" value=<%= user.Events[i].endTime %> /><br> <br>
            Time Zone: <input type="text" name="timeZone" value=<%= user.Events[i].timeZone %> /><br> <br>
            User Id: <input type="text" name="userId" value=<%=user.Events[i].userId %> /><br> <br>
        
            <button type="submit">Edit Event</button><br> <br>
        </form>
        <form action="/events/show/<%= user.Events[i].id %>?_method=DELETE" method="POST">
            <button type="submit">Delete Event</button>
        </form><br> <br>
        <% } %>
</body>
</html>